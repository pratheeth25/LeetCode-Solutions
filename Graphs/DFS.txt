class Solution {
    public ArrayList<Integer> dfs(ArrayList<ArrayList<Integer>> adj) {
        boolean[] vis = new boolean[adj.size()];
        Arrays.fill(vis,false);
        ArrayList<Integer> ans = new ArrayList<>();
        for(int i=0;i<adj.size();i++){
            if(!vis[i]){
                ccc(i,adj,ans,vis);
            }
        }
        return ans;
        
    }
    static void ccc(int node,ArrayList<ArrayList<Integer>> adj, ArrayList<Integer> ans,boolean[] vis){
        vis[node] = true;
        ans.add(node);
        for(int i:adj.get(node)){
            if(!vis[i]){
                ccc(i,adj,ans,vis);
            }
        }
    }
    
}
This solution is for disconnected components also .
